dependencies {
    compile project(':common')
    compile project(':database')
    compile group: 'io.vertx', name: 'vertx-mail-client', version: "${vertxVersion}"
}

assets {
    from 'src/main/generated/mail-js'
}