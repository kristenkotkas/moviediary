dependencies {
  compile project(':shared/common')
  compile group: 'io.vertx', name: 'vertx-service-proxy', version: "${cfg.ver.vertx}"
  compile group: 'io.vertx', name: 'vertx-sockjs-service-proxy', version: "${cfg.ver.vertx}"
  testCompile project(':shared/common')
}